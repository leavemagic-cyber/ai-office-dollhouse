# AI Office Dollhouse V3 — 7% 建築線稿透視樓板 畫風與動畫規格（草案）

> 狀態：**IMPLEMENTED v1.0 — Owner 2026-08-11 全數批准（①②③④⑤），規格凍結後已實作完成**。
> 實作結果、驗收證據與四項偏差見 §6.5；決議紀錄見 §7。
> 定位：**維持** `AI_OFFICE_DOLLHOUSE_DESIGN_SPEC.md` §0 與 V2 §0 的微型透明浮層契約
> （桌面左緣、約 7% 工作區寬、透明、active-only、無任務隱藏、唯讀低干擾）。
> 本文件**只重新定義**：①視覺畫風（建築鉛筆線稿軸測）②樓層插入/畫出動畫 ③微型人偶呈現。
> 修訂軌跡：v1 側面剖屋塔、v2 大型彩色軸測塔（300px）皆經 Owner 2026-08-11 指正作廢——
> Owner 要的是「佔螢幕 7%」「建築平面圖那種」「有任務把樓層插入／線稿畫出動畫」，
> 並提供線稿軸測參考圖（風格語彙：紙上鉛筆稿、細線樓板、虛線導引、微型家具人偶）。
> 草稿圖：`artifacts/concepts/v4-pureline-figures-20260811.html`（現行 v4.1）；v3 淡彩對照稿同目錄。

## 1. 不變的契約（照抄 §0，實作不得漂移）

- 桌面左緣直立微型浮層，預設約 **7% 工作區寬**（1920 → ~135px）；透明背景、無邊框。
- **active-only**：只有可靠 live session／近期工作快照／短時重要事件／Owner 請示才產生樓板；
  閒置 Provider、空 Lobby、純 presence 不出現；無任何有用樓板時整個視窗隱藏、不搶焦點。
- 唯讀觀察器；不虛構人員；輕量可降載；樓板外點擊穿透。

## 2. 畫風：建築鉛筆線稿軸測（新）

| 項 | 規格 |
|---|---|
| 投影 | 2:1 dimetric（screenX=x−y、screenY=(x+y)/2−z）；通用製圖技法，非任何遊戲 tile 複製 |
| 樓板 | **純線框（Owner 2026-08-11 定案①）**：無填色，石墨描邊 0.7–0.9px＋厚度雙緣線＋角落短豎線；描圖紙細格線（石墨 14% 透明度） |
| 尺寸 | 每板實機約 **128×64px**；板距約 52px；高度隨樓板數伸縮 |
| 垂直語彙 | 樓板四角**虛線導引線** `#A89C86`（2-3 dash）串起樓層；**右後導引線＝電梯軌**（線框小廂沿線行進，方案 §10.4 共享跨樓佇列、A/C/I 載具）；右前角三階小樓梯＝板上進出點 |
| 佈局 | **每層是一間辦公室（Owner 2026-08-11 指正⑤）**：高家具靠後牆（白板/檔案櫃/審閱架）→ 中段軸線對齊工作區 → 前緣走道通樓梯；團隊樓層＝經理桌（面向團隊）＋SessionPod 工作島（**每 session 一島**，SPEC:126-128 §4.1、SPEC:144-147 §4.3、SPEC:164-173 §4.5）；Owner 室＝辦公大桌＋收件匣＋高背椅＋白板＋請示等候椅×2（SPEC:131-137）；Claude＝審閱長桌＋審閱架×2＋印章站（E/F 印章動畫道具）；**禁止散落道具** |
| 家具 | 全線框（桌＝頂面菱形＋三腳線＋立式小螢幕框；櫃架＝箱框＋層線＋書冊刻線）；每層 4–6 組、一律對齊 2:1 軸線 |
| 名牌 | 樓板左下小字（實機 7px）`PROVIDER・用途`＋2px 狀態點；不做大標籤 |
| 對比策略 | **自動墨稿/白稿（定案③）**：取樓板背後桌布平均亮度，亮→石墨墨稿、暗→白稿 `#EDE8DA`（狀態色同步提亮）；使用者可手動鎖定 |
| 色盤 | 石墨 `#6E655A`、輔線 `#9A907F`、白稿 `#EDE8DA`、虛線 `#A89C86`；識別色 Codex `#4A6FA5`／Claude `#6B8F71`／Gemini `#B8860B`／Grok `#8B5A6B`（僅用於 2px 狀態點與小人胸口 1px 身分點；刻意非官方品牌色）；狀態 `#3D7A5A`/`#C4A35A`/`#B54A4A`（純線版無家具淡彩） |

## 3. 微型積木人偶（新呈現，IP 語彙不變）

- 7% 尺度（Owner 2026-08-11 **定案②：A 建築線稿小人**；方頭小人作廢）：
  細身 7 頭身、橢圓小頭、錐形軀幹線框、細線四肢、高 13px、胸口 1px provider 色點＝身分；
  走路＝前後腳清楚交換（V2:119 照抄）＋單臂前擺；備援：深色/雜亂桌布可切 C 石墨剪影。
  **美感底線（Owner 明示）**：會出現在動畫的每個元素都要過美感關，不合格不得上畫面。
- 使用者把視窗拉大（>2 倍）才切換 28px 細節版（沿用既有 11-prism 骨架與四材質家族語彙）。
- IP 禁令全承接：無凸粒、無 C 型手、無圓柱頭、非經典亮黃、無 minifigure 輪廓、無品牌色/Logo/吉祥物、不仿既有遊戲 sprite/tile、零外部素材。
- 人偶數＝真實 session/事件；閒置＝樓板不存在（active-only），故無「壓暗空房」問題。

## 4. 動畫（正本＝既有設計方案，本稿只做線稿化＋補缺口）

> **2026-08-11 Owner 指正後修正**：動畫/編排的正本是 repo 既有方案——SPEC §5.1-5.3（施工/收樓）、
> §7（A–J 十組招牌動畫，SPEC:245-287＋V2:144-155 明文「全部保留」）、§8（工作/討論/閒置）、
> §9.1（請示：公事包、敲門三下、排隊）、§10.3-10.4（P0–P4 優先級、6–8 位移上限、單一主演出佇列、
> 閒置 5FPS/休眠、電梯＝共享跨樓移動佇列）；V2 §7.1-7.4（常態五類含準備/收尾動作、
> 「不得只准一人動」「不得用粗糙幀數冒充效能」）。**本稿不改寫上述任何內容。**

| 項 | 線稿化呈現 |
|---|---|
| 樓層出現 | 方案 §5.1/§5.2 線稿化：藍圖畫出（=「藍圖淡入」墨稿版）→ 線框小吊車吊入 → 滑入鎖定讓位 → 小人進駐 |
| 樓層收起 | 方案 §5.3：收拾 → 內裝逐筆擦除 → 樓板收起 → 其餘合攏；不補播離線動畫 |
| 招牌 A–J | 全數保留、13px 線稿小人＋文件卡/印章點/「?」標籤微縮演出；同時 1 個主演出（§10.4 佇列） |
| 常態動畫 | V2 §7.1 五類照做（打字/翻頁/白板/交件/巡視，含準備與收尾動作） |
| 請示 | §9.1／G：沿電梯導引線上 Owner 板、門前排隊＋「?」紙標籤；勿擾＝只留紅點 |
| 電梯 | 右後虛線導引＝電梯軌，線框小廂行進（§10.4 共享佇列；A/C/I 的載具） |
| 真實性 | SPEC:517-523 照抄：completed 只由明確事件、presence-only 不播工作動畫、unknown 灰問號 |
| **文件缺口（本稿補）** | 方案無時長/幀數/easing、無正式事件→動畫映射表（映射在 code，Stage3 PASS）→ 補：畫出 0.8s／滑入 0.4s／進場 0.3s（待 Owner 認可）；實作維持 code 內單一映射表 |

## 5. 技術注意（給實作）

- 單一 canvas＋rAF；DPR 對齊（0.5px 線位移）避免細線糊化；線稿動畫用 Path2D 分段進度繪製。
- 既有 `prism()`／11-prism 人偶骨架保留為「放大模式」資產；7% 模式走新的線稿繪製函式。
- `floorSpecsForModel(..., { activeOnly: true })` 行為不變；新增插入/移除的過場狀態機（進場中/常駐/離場中）。
- 效能預算不變：動畫只在有活躍樓板時運行；`npm test`／`npm run check` 全綠＋補樓板過場回歸測試。

## 6. 驗收條件（可機械判定，給 Opus 5）

1. 預設視窗寬 = 工作區 7%±1%（1920 實測 128–142px）；無任務時視窗隱藏（`hidden` 狀態可查）。
2. 每塊活躍樓板為 2:1 軸測**純線框**：石墨描邊＋厚度雙緣線、右前角樓梯、四角虛線導引＋右後電梯軌與線框小廂；深/淺兩種桌布下自動墨稿/白稿反色（兩種桌布各截圖抽查）。
3. **辦公室佈局**：團隊樓層可見「後牆高家具＋SessionPod 工作島＋經理桌＋前緣走道」分區；工作島數＝該 Provider 真實 session 數；Owner 室含辦公桌＋收件匣＋請示等候椅（截圖對照 §2 佈局列抽查）。
4. 新任務事件 → 樓板以「藍圖畫出＋吊入滑入讓位」出現（過場 ≤1.5s）；任務結束 → 反向擦除收起；過場期間其餘樓板平滑讓位/合攏。
5. 人偶為 13px A 案建築線稿小人（細身 7 頭身＋胸口色點）；抽查無凸粒/C 型手/圓柱頭/亮黃膚/品牌色。
6. working/waiting/error 三狀態各有可觀察微動畫與狀態點；A–J 招牌動畫至少 A（進場）、G（請示）、E/F（印章）可由模擬事件流觸發；閒置時 rAF 停止或 ≤2fps。
7. 全程無外部資產；`npm test`、`npm run check` 全綠；新增過場狀態機與佈局回歸測試。

## 6.5 實作結果與偏差（2026-08-11 Claude Opus 5 實作、自驗）

**實作檔案**：新 `resources/js/sketch.js`（線稿繪製核心／純函式可測）、改寫 `resources/js/renderer.js`
（保留 `ROOM_META` 與 `RoomRenderer` 六方法契約，新增 `setPhase` / `setTheme`）、
`resources/js/main.js`（7% 寬、DPR 超取樣畫布、進場/離場狀態機、主題注入、視窗高度）、
`resources/styles.css`、`neutralino.config.json`、新 `scripts/desktop-luminance.ps1`（桌布亮度探針）＋
`native-bridge.js` 的 `desktopLuminance()`＋`package-release.ps1` 登記、新 `tests/sketch.test.mjs`（11 條）。

**驗收結果**：`npm test` 67/67 通過（原 56 ＋ 新 11）；`npm run check` 通過（148 檔／20 JS／0 音訊）。
實機（`npx neu run` ＋ 注入測試事件）已驗：無工作時視窗隱藏、有事件自動顯示、三層樓板同時顯示且
名牌未被裁切、視窗背景全透明（桌面內容透出）、白稿在黑桌面正確。證據圖：`artifacts/v4-sketch/`。

**與規格的偏差（誠實記錄，未經 Owner 同意不擴大）**：

1. **7% 寬度在本機被下限夾住**：本機工作區寬 1493 邏輯 px，7% = 104.5px，但既有下限
   `MIN_OVERLAY_WIDTH = 118` 使實際為 118px（工作區 7.9%，仍在驗收的 7%±1% 內）。
   維持 118 下限是為了小字名牌可讀；若 Owner 要嚴格 7%，須一併降低下限並接受名牌縮到 ~5px。
2. **真實性標籤改法**：舊版每個人偶掛 `L1/S1` 文字標籤，與「不做大標籤」的凍結畫風衝突；
   改為「近期快照人偶用虛線筆觸、live 用實線」＋樓板名牌與狀態點。語意不減，雜訊減少。
3. **密集情況仍可能同縱列前後站**：同一 pod 有 4 人以上時，最後兩個座位會落在同一螢幕縱列
   （前後相距 12.4px，靠深度排序遮擋）。≤3 人的常態已由測試保證不重疊。
4. **未做**：`agy`/Codex 等第三方 CLI 觀點在本輪未再諮詢（Codex 額度、Gemini 已改 Antigravity）。

## 6.6 第二輪實作與偏差（2026-08-11 晚，D1-D6，Opus 5）

依 `AI_OFFICE_DOLLHOUSE_V5_MASTER_PLAN_20260811.md` 的 D1-D6 執行，Codex CLI 兩輪審查。
**推翻本規格第 7 節第 2 點的「胸口色點＝身分」**：胸口色點已移除，改為腳下身分色條。

**本輪偏差（誠實記錄）**：

1. **人偶全高 13.3px 而非規格的 13.0**：頸隙加到 0.95 才不會讓實心頭與肩線黏成一坨。
   Codex 第一輪判定可接受（可視縫約 0.48px）；第二輪它反過來要求縮回 0.5，與自己前一輪矛盾，
   我維持 0.95 並附實圖理由。
2. **走路步幅 20 度**（規格 22-30）：2:1 軸測是壓扁的正面視角，26 度看起來像劈腿。Codex 同意。
3. **不做像素中心對齊**：畫布 2-4 倍超取樣＋非整數 CSS 縮放＋人偶連續移動，硬對齊會抖。Codex 同意。
4. **坐姿只畫近側手臂**：遠臂無論放哪都會跨過脊椎或撞上椅背（兩種都試過並留圖），
   四分之三視角下省略遠臂是唯一乾淨解。Codex 給的座標未採用，理由附實圖。
5. **樓層語意改變（推翻舊的「每 14 人一個 annex」）**：改成每個帶 subagent 的 session 一層、
   其餘併入共用辦公層。`floor-layout.js` 的 8 條測試已依新語意改寫，不是刪除。
6. **單層容量 12 → 13**：只計正式工位（3 島 × 4 ＋ Owner 桌），會議椅／接待椅不計。
7. **未做**：本輪視覺驗證只走「本地伺服器載入真實模組截圖」，沒有再跑 `npx neu run` 實機路徑；
   桌邊切線重疊只保證了椅背側，桌側取決於座位與桌的相對位置，未做全面掃描。

## 7. Owner 決議紀錄（2026-08-11）

1. 版本：**純線條版**——定案。
2. 小人：**A 建築線稿小人**——定案；胸口色點＝身分。
3. 深淺桌布：**自動墨稿/白稿反色**——定案。
4. 動畫：對齊既有方案＋補時長參數 0.8/0.4/0.3s——定案。
5. 辦公室佈局：Owner 指正「擺設沒有設計成辦公室」→ 已依 SPEC §4.1/§4.3/§4.5 重排
   （見 §2 佈局列與 v4 草稿圖第 2 節）——**待 Owner 確認；確認後全規格凍結、交 Opus 5 實作**。
